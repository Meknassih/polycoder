import base64decode from './base64decode.html';
import Component from '../lib/component';

export default class Base64Decode extends Component {
    constructor() {
        super(base64decode);
    }
}